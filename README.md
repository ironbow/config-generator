```
usage: config-generator.py [-h] [-s SOURCE] [-t TEMPLATE]

Generate device configurations from templates.

optional arguments:
  -h, --help            show this help message and exit
  -s SOURCE, --source SOURCE
                        Path to file containing source data (.yaml).
  -t TEMPLATE, --template TEMPLATE
                        Path to file containing Jinja template (.j2).
```